# 首页结构

1. 顶部跑马灯：普通文档流元素，连续水平滚动参考站的 Creative Pass 文案。
2. 主导航：普通文档流元素，不吸顶；桌面行高按参考截图复刻。
3. 筛选栏：Awards、Categories、Tags、Technologies、Countries、Font Collections、Colors 与 Reset filters 完整复刻。
4. Skill 网格：沿用既有卡片组件，随页面自然滚动。
5. 详情与上传：沿用既有模态层。

交互模型：导航为悬停交互；筛选为点击展开；卡片为悬停和点击；页面为浏览器原生滚动。
